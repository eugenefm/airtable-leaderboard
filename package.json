{
  "name": "airtable-leaderboard",
  "version": "1.0.0",
  "description": "A live leaderboard app that displays data from airtable",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "airtable": "^0.5.6",
    "bottleneck": "^2.4.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "js-beautify": "^1.7.5",
    "showdown": "^1.8.6"
  }
}
